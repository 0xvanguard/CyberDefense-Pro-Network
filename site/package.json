{
  "name": "cdpn-site",
  "version": "1.0.0",
  "description": "CDPN - CyberDefense Pro Network Website",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev content",
    "build": "cd content && cp -r ../.vitepress . && npx vitepress build && cd .. && node postbuild.cjs",
    "pull": "node sync.cjs pull",
    "status": "node sync.cjs status",
    "preview": "vitepress preview content",
    "sync:pull": "node sync.cjs pull",
    "sync:status": "node sync.cjs status"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
